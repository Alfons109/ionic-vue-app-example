import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joefresh.ionic',
  appName: 'Joe Fresh',
  webDir: 'dist',
  bundledWebRuntime: false 
};

export default config;
